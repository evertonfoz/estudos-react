import { createTheme } from "@mui/material";

export const AppTheme = createTheme({
    palette: {
        mode: "light",
        primary: {
            main: "#DF9E1C",
        },
        secondary: {
            main: "#fbf1e2",
        },
        text: {
            primary: "#02374B",
            secondary: "#000000"
        }
    },
});
